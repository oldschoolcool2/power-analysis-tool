% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_matched_case_control.R
\name{validate_matched_case_control_inputs}
\alias{validate_matched_case_control_inputs}
\title{Validate Matched Case-Control Inputs}
\usage{
validate_matched_case_control_inputs(
  n_pairs = NULL,
  or_value = NULL,
  p0 = NULL,
  alpha = NULL,
  power = NULL,
  ratio = NULL,
  sided = NULL,
  calc_mode = "calc_n"
)
}
\arguments{
\item{n_pairs}{Number of matched pairs (for power/MDE calculations)}

\item{or_value}{Odds ratio}

\item{p0}{Exposure probability in controls (\%)}

\item{alpha}{Significance level (0-1)}

\item{power}{Power level (0-100)}

\item{ratio}{Controls per case (matching ratio)}

\item{sided}{Test type ("two.sided" or "one.sided")}

\item{calc_mode}{Calculation mode ("calc_n", "calc_power", "calc_effect")}
}
\value{
List with components:
  \describe{
    \item{valid}{Logical - TRUE if all validations passed}
    \item{messages}{Character vector of all messages}
    \item{errors}{Character vector of error messages only}
    \item{warnings}{Character vector of warning messages only}
    \item{notes}{Character vector of informational messages only}
  }
}
\description{
Validates all inputs for matched case-control power, sample size, and effect size calculations.
Returns a structured validation result with errors, warnings, and notes.
}
\examples{
\dontrun{
# Valid sample size calculation
validate_matched_case_control_inputs(
  or_value = 2.0, p0 = 20, alpha = 0.05, power = 80,
  ratio = 1, sided = "two.sided", calc_mode = "calc_n"
)

# Invalid: OR = 1 (null hypothesis)
validate_matched_case_control_inputs(
  or_value = 1.0, p0 = 20, alpha = 0.05, power = 80,
  ratio = 1, sided = "two.sided", calc_mode = "calc_n"
)
}
}
