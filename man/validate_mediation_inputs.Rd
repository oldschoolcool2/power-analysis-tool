% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_mediation_validation.R
\name{validate_mediation_inputs}
\alias{validate_mediation_inputs}
\title{Validate Mediation Analysis Inputs}
\usage{
validate_mediation_inputs(
  n = NULL,
  power = NULL,
  path_a = NULL,
  path_b = NULL,
  path_c_prime = NULL,
  se_a = NULL,
  se_b = NULL,
  alpha = NULL,
  sided = NULL,
  calc_mode = "calc_power"
)
}
\arguments{
\item{n}{Sample size (for power/MDE calculations)}

\item{power}{Power level (0-100)}

\item{path_a}{Standardized path coefficient from X to M}

\item{path_b}{Standardized path coefficient from M to Y|X}

\item{path_c_prime}{Direct effect from X to Y|M (optional)}

\item{se_a}{Standard error of path a (optional)}

\item{se_b}{Standard error of path b (optional)}

\item{alpha}{Significance level (0-1)}

\item{sided}{Test type ("two.sided" or "one.sided")}

\item{calc_mode}{Calculation mode ("calc_power", "calc_n", or "calc_mde")}
}
\value{
List with components:
  \describe{
    \item{valid}{Logical - TRUE if all validations passed}
    \item{messages}{Character vector of all messages}
    \item{errors}{Character vector of error messages only}
    \item{warnings}{Character vector of warning messages only}
    \item{notes}{Character vector of informational messages only}
  }
}
\description{
Validates all inputs for mediation analysis power, sample size, and MDE calculations.
Returns a structured validation result with errors, warnings, and notes.
}
\examples{
\dontrun{
# Valid power calculation
validate_mediation_inputs(
  n = 200, path_a = 0.3, path_b = 0.3, alpha = 0.05,
  sided = "two.sided", calc_mode = "calc_power"
)

# Invalid: path coefficients outside realistic range
validate_mediation_inputs(
  n = 200, path_a = 5.0, path_b = 0.3, alpha = 0.05,
  sided = "two.sided", calc_mode = "calc_power"
)
}
}
