% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_non_inferiority.R
\name{validate_non_inferiority_inputs}
\alias{validate_non_inferiority_inputs}
\title{Validate Non-Inferiority Inputs}
\usage{
validate_non_inferiority_inputs(
  p1 = NULL,
  p2 = NULL,
  margin = NULL,
  n1_fixed = NULL,
  alpha = NULL,
  power = NULL,
  ratio = NULL,
  calc_mode = "calc_n"
)
}
\arguments{
\item{p1}{Event rate in test group (\%)}

\item{p2}{Event rate in reference group (\%)}

\item{margin}{Non-inferiority margin (percentage points)}

\item{n1_fixed}{Fixed sample size in test group (for margin calculation)}

\item{alpha}{Significance level (0-1)}

\item{power}{Power level (0-100)}

\item{ratio}{Allocation ratio (n2/n1)}

\item{calc_mode}{Calculation mode ("calc_n" or "calc_effect")}
}
\value{
List with components:
  \describe{
    \item{valid}{Logical - TRUE if all validations passed}
    \item{messages}{Character vector of all messages}
    \item{errors}{Character vector of error messages only}
    \item{warnings}{Character vector of warning messages only}
    \item{notes}{Character vector of informational messages only}
  }
}
\description{
Validates all inputs for non-inferiority testing calculations.
Returns a structured validation result with errors, warnings, and notes.
}
\examples{
\dontrun{
# Valid sample size calculation
validate_non_inferiority_inputs(
  p1 = 10, p2 = 10, margin = 5, alpha = 0.025, power = 80,
  ratio = 1, calc_mode = "calc_n"
)

# Invalid: margin too large
validate_non_inferiority_inputs(
  p1 = 10, p2 = 10, margin = 25, alpha = 0.025, power = 80,
  ratio = 1, calc_mode = "calc_n"
)
}
}
