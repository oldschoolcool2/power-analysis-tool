% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_survival_equivalence.R
\name{validate_survival_equivalence_inputs}
\alias{validate_survival_equivalence_inputs}
\title{Validate Survival Equivalence/Non-Inferiority Inputs}
\usage{
validate_survival_equivalence_inputs(
  test_type = NULL,
  calc_mode = NULL,
  power = NULL,
  hr_expected = NULL,
  hr_margin_ni = NULL,
  hr_margin_equiv = NULL,
  n_fixed = NULL,
  prop_exposed = NULL,
  event_rate = NULL,
  allocation_ratio = NULL,
  alpha = NULL
)
}
\arguments{
\item{test_type}{Test type ("non-inferiority" or "equivalence")}

\item{calc_mode}{Calculation mode ("calc_n" or "calc_margin")}

\item{power}{Power level (0-100)}

\item{hr_expected}{Expected hazard ratio}

\item{hr_margin_ni}{Non-inferiority margin (HR > 1)}

\item{hr_margin_equiv}{Equivalence margin (HR > 1)}

\item{n_fixed}{Fixed sample size (for margin calculation)}

\item{prop_exposed}{Proportion exposed/treated (\%)}

\item{event_rate}{Overall event rate (\%)}

\item{allocation_ratio}{Allocation ratio (n2/n1)}

\item{alpha}{Significance level (0-1)}
}
\value{
List with components:
  \describe{
    \item{valid}{Logical - TRUE if all validations passed}
    \item{messages}{Character vector of all messages}
    \item{errors}{Character vector of error messages only}
    \item{warnings}{Character vector of warning messages only}
    \item{notes}{Character vector of informational messages only}
  }
}
\description{
Validates all inputs for time-to-event equivalence and non-inferiority calculations.
Returns a structured validation result with errors, warnings, and notes.
}
\examples{
\dontrun{
# Valid non-inferiority sample size calculation
validate_survival_equivalence_inputs(
  test_type = "non-inferiority", calc_mode = "calc_n",
  power = 80, hr_expected = 0.95, hr_margin_ni = 1.25,
  prop_exposed = 50, event_rate = 30, allocation_ratio = 1, alpha = 0.025
)

# Invalid: expected HR worse than margin
validate_survival_equivalence_inputs(
  test_type = "non-inferiority", calc_mode = "calc_n",
  power = 80, hr_expected = 1.30, hr_margin_ni = 1.25,
  prop_exposed = 50, event_rate = 30, allocation_ratio = 1, alpha = 0.025
)
}
}
