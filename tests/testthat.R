# This file is part of the standard testthat testing setup
# It allows running tests with: devtools::test() or testthat::test_dir()

library(testthat)

# Run all tests in the testthat directory
test_check("power-analysis-tool")
